# 策略库日调度工作说明 — 静态库 → 调度版 → MSAI

## 1. 工作概述

我的工作是构建并打通一条**泵站日调度策略流水线**,将静态策略库转化为带时间排班、可被客户端直接使用的 MSAI 推理文件:

```
close7_20260810.parquet (静态策略库)
        │  ① 日调度  schedule_daily_strategy_library.py
        ▼
close7_20260810_scheduled.parquet (调度版策略库)
        │  ② MSAI 转化  train_msai.py
        ▼
close7_20260810_scheduled.msai (客户推理文件)
```

三个环节分别承担:候选方案生成(策略库网格)→ 时间排班(双向 DP 全局最优)→ 推理格式封装(内存数组,免解析)。

## 2. ① 日调度(schedule_daily_strategy_library.py)

### 做了什么

1. **流量/压力预测**:Transformer(30 分钟重采样)预测未来 48 个时间点(30 min × 24 h)的总管流量与分时压力;
2. **逐点策略库匹配**:每个时间点按 (预测流量, 预测压力) 查询静态库:
   - 流量 ∈ `[Flow_Lower, Flow_Upper)`、压力 ∈ `[Header_Press_Lower, Header_Press_Upper)`(流量 50 m³/h 一档、压力 0.01 MPa 一档);
   - **液位不参与筛选**,每个 (时间点, Time_Level) 尽量覆盖全部液位档(缺档从同点匹配池借其他泵组的真实行);
   - 无精确匹配时按压力/流量档偏移(距离 1~3)逐级放宽并告警;每点取前 50 个泵组作为 DP 候选;
3. **双向动态规划全局选策**:前向 + 后向 DP 求每个候选的「必经总代价」(最优前缀 + 自身节点 + 最优后缀),同一时间点候选按代价升序排名 → `Time_Level`(1 = 全局最优路径所用,2 = 次优,…);
   - 节点代价 = −效率 × w_eff + 大泵 × w_large + |预测流量 − 目标流量| × w_flow(目标流量 = 策略流量区间中点);
   - 切换代价 = 泵状态翻转 × w_state + 频率变化 × w_freq;首层计入 `pump_run_hours.csv` 的当前泵组状态(从正在运行的泵组出发);
   - 逐点模拟各泵连续运行小时,达 `--max_run_hours` 上限的点强制停该泵并从候选剔除后重解;日末回写 `pump_run_hours.csv`(跨天累加、停泵归 0);
4. **回写策略库**:被选中策略填 `Suggest_Runtime_Start / End / Time_Level`,一行 = 一个 (策略, 时间点, 第 N 优解) 使用记录;同一策略用于多个时间点时复制策略行(每行一个时段);未被选中策略 `Time_Level = 0 → 1000`(未使用标记)。

### 输出文件

| 文件 | 内容 |
|---|---|
| `strategy/close7_<日期>_scheduled.parquet` | 策略库副本(全量行 + 排班回写,即本流水线的 ② 输入) |
| `strategy/close7_<日期>_scheduled.csv` | 已用策略子集(仅 `Time_Level < 1000`) |
| `daily_pump_schedule_strategy_lib.csv` | DP 全局最优路径(逐时间点) |
| `daily_pump_schedule_strategy_lib_candidates.csv` | 每点候选按液位展开(含借档行) |
| `daily_pump_schedule_strategy_lib_blocks.csv` | 相同 (泵组, 频率) 连续时段合并块 |

**时段约定**:预测时刻 t 在窗口 [t−15 min, t+15 min) 内生效,`Suggest_Runtime_Start/End` 为该窗口的 **HHMM 整数**(如 00:30 点 → 0015/0045;245 即 02:45)。因此所有时段真实时长均为 **30 分钟**,存储为整数时前导零被丢弃。

## 3. ② MSAI 转化(train_msai.py)

```bash
python train_msai.py strategy/close7_20260810_scheduled.parquet strategy/close7_20260810_scheduled.msai [--force]
```

### 做了什么

1. **校验与归一化**:检查必需字段(压力/流量边界、PCTW、Eff、Pump_Group),缺失可选字段填默认值(`Time_Level=50`、运行时窗口 0/2500、液位 0–100、Pump_Exclusivity=9999);非法 Pump_Group 行剔除;
2. **类型压缩**:各字段按语义压成 `float16 / float32 / uint8 / uint16 / uint32`(如流量/液位 float16、Time_Level uint8、运行时窗口 uint16),内存占用最小化;
3. **泵数量推断**:由 Pump_Group 二进制串长度与 P1~P7_Freq/_Eff 列号取最大值(支持 1–32 泵);
4. **序列化**:全部字段转为 numpy 数组,连同每列 min/max 统计、`freq_cols` 清单、样本数等元数据打包,`joblib.dump(compress=3)` 原子写入(临时文件 + os.replace,失败自动清理)。

最终 `.msai` 文件是**内存直接加载的推理模型**(非 CSV/JSON 需解析),客户推理端 `joblib.load` 后按数组查表即可。

## 4. 两个 parquet 文件的定位与关系

| | 静态策略库 `close7_20260810.parquet` | 调度版策略库 `close7_20260810_scheduled.parquet` |
|---|---|---|
| 定位 | 候选策略网格(策略库本体) | 策略库 + 日调度方案(执行版) |
| 行数 | 997,543 | 1,022,952(多 25,409 行) |
| `Time_Level` | 全部为 0(未排期) | 1–50(被选中的策略排名)或 1000(未使用哨兵) |
| `Suggest_Runtime_Start/End` | 全部为 0–2359(全天占位) | 具体 30 分钟执行时段(HHMM,如 0245–0315) |
| id 唯一性 | 全部唯一 | 12,705 个 id 被拆分(最多 85 行,平均 3 行) |
| 物理参数列(泵组、频率、效率、流量/压力/液位边界、PCTW 等) | — | **与静态库完全一致,未做任何修改** |

调度版 = 静态库全量行 + 被选中策略的具体执行时段与优先级排名;唯一改写的只有 `Time_Level`、`Suggest_Runtime_Start`、`Suggest_Runtime_End` 三列,其余 29 列原样保留。

## 5. 如何使用生成的 parquet 文件

### 5.1 静态库(候选查询)

- **选泵查询**:按当前工况(流量、压力)区间匹配一行或多行,液位区间用于细分液位档:
  ```sql
  WHERE Flow_Lower <= 当前流量 < Flow_Upper
    AND Header_Press_Lower <= 当前压力 < Header_Press_Upper
  ```
  每行给出 `Pump_Group`、各泵频率/效率、`PCTW`、`Eff` 等完整参数。
- **适用场景**:训练/离线评估、无预测条件时的纯工况匹配、日调度脚本的候选来源。
- 静态库**没有时间维度**,`Suggest_Runtime_*`/`Time_Level` 均为占位(0/0–2359),不能直接排班。

### 5.2 调度版(日排班执行)

在静态库查询基础上追加时间与优先级两个维度:

- **按时段取行**:
  ```sql
  WHERE Flow_Lower <= 当前流量 < Flow_Upper
    AND Header_Press_Lower <= 当前压力 < Header_Press_Upper
    AND Suggest_Runtime_Start <= 当前时刻(HHMM) < Suggest_Runtime_End
  ```
  (跨零点时段如 2345→0015 需按 23:45–次日 00:15 处理)
- **按排名取优**:同一工况/时段下多行时,按 `Time_Level` 升序取策略(1 = 全局最优路径,2 = 次优,…);最优方案失败时逐级降级切换。
- **多行同 id**:同一策略一天内被安排到多个时段,每行一个时段,物理参数相同,**逐行执行,不要按 id 去重**。
- **`Time_Level = 1000` 兜底行**:未排入任何时段的策略,窗口保持 0–2359,全天可用,作为计划被扰动时的回退候选;筛选已排方案用 `Time_Level < 1000`。
- **`close7_<日期>_scheduled.csv`** 是调度版的精简子集,只含已排方案,供无需兜底场景直接使用。

### 5.3 一个完整的执行示例

```
当前 04:10,流量 1970 m³/h,压力 x,时段 0245–0315 与 0315–0345 已排(策略 id=358536, Time_Level 37/38)
→ 04:10 落在 0315–0345 时段 → 取该行 → Pump_Group=0000111 + 各泵频率/效率 → 下发执行
→ 若该泵组不可用 → 降级取 Time_Level 38 → 仍不可用 → 取本工况 Time_Level=1000 的兜底行
```

## 6. 调度版相比静态库的优势

1. **从「该选什么」升级为「几点开、开多久」**:静态库无时间维度无法直接执行;调度版输出即执行指令。
2. **全天排班,均匀覆盖**:49,455 条被排时段(24,046 个唯一策略)均匀分布在一整天,每 2 小时约 4,200 条(18:00–22:00 略少),各时段都有可用方案;统一 30 分钟粒度,执行节奏规整。
3. **全局最优,而非逐时段贪心**:双向 DP 保证时段间衔接最优,从「当前正在运行的泵组」出发,并受连续运行时长上限约束,避免局部最优与泵过劳。
4. **一库两用:选中方案 + 兜底候选并存**:未选中策略标记 1000、保持全天窗口;实际工况偏离计划时可随时回退换策略,不丢覆盖;行级保留原 id,与静态库一一对应,可审计「哪些策略被用、用了几个时段、排到几点」。
5. **一次多时段复用,自动展开**:12,705 个策略被排到多个时段(平均 3 段,最多 85 段),复制为多行每行一个时段,执行器逐行读取即可。
6. **支持跨零点过夜时段**:1,050 条窗口为 2345→0015(23:45–次日 00:15),排班可跨天连续。

## 7. 被改写三列各自的优势

### 7.1 `Time_Level` — 优先级排名
- **主备切换**:1 = 全局最优,2…50 = 依次降级;策略切换按排名逐级降级,保证「最优方案失败时有明确备选顺序」;
- **被禁开/未使用可区分**:未选中标记为 1000(哨兵),不会与已排方案混淆;
- **液位全档覆盖**:每个 (时段, 排名) 尽量覆盖全部液位档,任何液位状态都有可用策略。

### 7.2 `Suggest_Runtime_Start / End` — 精确执行时段
- **HHMM 分钟精度**:如 0245–0315 = 02:45–03:15,可直接下发设备或生成值班表;
- **逐段可执行**:一行一段,启停时间一目了然;同一策略多次启停即查即用;
- **跨零点**:结束 < 开始的时段表示过夜(2345→0015 为 30 分钟),排班可跨天。

### 7.3 三列组合
策略(泵组/频率/效率)+ 时段(启停)+ 排名(主备顺序)闭环,构成**可执行 → 可切换 → 可审计 → 可兜底**的完整日调度方案;调度版是唯一可直接进入 `train_msai.py` 生成客户推理文件的版本(静态库全部 `Time_Level=0`,MSAI 端无法区分优先级)。

## 8. 数据统计附录

| 指标 | 数值 |
|---|---|
| 静态库行数 / 调度版行数 | 997,543 / 1,022,952 |
| 被选中策略行(Time_Level 1–50) | 49,455 行(24,046 个唯一 id,占静态库 2.4%) |
| 未使用哨兵(Time_Level = 1000) | 973,497 行(窗口保持 0–2359) |
| 排名档位数 | 50(1 = 全局最优路径) |
| 被拆分策略数 | 12,705 个 id(平均 3 行,最多 85 行) |
| 时段真实时长 | 全部 30 分钟(48 点 × 30 min 网格) |
| 时段起始分布 | 全天均匀,每 2 小时约 4,200 条(18:00–22:00 略少) |
| 跨零点窗口 | 1,050 条(2345→0015) |

## 9. 使用注意事项

1. **时段值是 HHMM 整数,不是分钟数**:245 = 02:45(前导零被 int 丢弃);跨零点时段结束 < 开始(2345→0015),时长按跨天计算;
2. **`Time_Level = 1000` 是哨兵值**:表示未选中/未使用,不是排名 1000;筛选已排方案用 `< 1000`;
3. **多行同 id 物理参数完全一致**:仅时段与排名不同,逐行执行,勿按 id 去重丢掉时段;
4. **调度依赖预测与运行状态**:结果基于 Transformer 预测与 `pump_run_hours.csv` 当前泵态,日末回写运行时长,需保证 `pump_run_hours.csv` 的连续性;
5. **MSAI 转化要求**:输入必须 .parquet,输出必须 .msai;已存在输出需 `--force`;物理字段缺失时按 `OPTIONAL_FIELD_DEFAULTS` 兜底(`Time_Level` 缺失默认 50)。
